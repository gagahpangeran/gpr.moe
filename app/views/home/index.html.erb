<% content_for :title, "Home" %>

<h1>GPR.moe</h1>

<% if authenticated? %>
  <p>Hello, <strong><%= Current.user.username %></strong>!</p>
  <%= button_to "Logout", logout_path, method: :delete %>
<% else %>
  <%= link_to "Login", login_path %>
<% end %>
