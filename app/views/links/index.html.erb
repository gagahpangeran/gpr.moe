<% content_for :title, "All Links" %>

<h1>All Links</h1>

<%= link_to "Add new link", new_link_path %>

<% @links.each do |link| %>
<div>
  <strong>Slug:</strong> <%= link.slug %>
  <strong>URL:</strong> <%= link.url %>
<div>
<% end %>
